services:
  postgres_db:
    build:
      context: ./db
      dockerfile: postgres/Dockerfile
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    expose:
      - "5432"
    restart: unless-stopped
    stop_grace_period: 30s
    mem_limit: 512m
    networks:
      - alltura_network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME} -h 127.0.0.1 -p 5432 || exit 1" ]
      interval: 15s
      timeout: 10s
      retries: 6
      start_period: 40s
  redis:
    image: redis:7-alpine
    command:
      - redis-server
      - --appendonly
      - "yes"
      - --appendfsync
      - everysec
      - --maxmemory
      - 256mb
      - --maxmemory-policy
      - allkeys-lru
    volumes:
      - redis_data:/data
    expose:
      - "6379"
    restart: unless-stopped
    stop_grace_period: 15s
    mem_limit: 300m
    networks:
      - alltura_network
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        NODE_ENV: ${NODE_ENV:-production}
    environment:
      # DB
      DB_HOST: ${DB_HOST:-postgres_db}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_PORT: ${DB_PORT:-5432}

      # Redis
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}

      # App
      NODE_ENV: ${NODE_ENV:-production}
      PORT: ${PORT:-5000}
      CLIENT_URL: ${CLIENT_URL}
      GOOGLE_APPLICATION_CREDENTIALS: ${GOOGLE_APPLICATION_CREDENTIALS:-/app/service-account.json}
      JWT_SECRET: ${JWT_SECRET}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET}
      SESSION_SECRET: ${SESSION_SECRET}
      VAPID_PUBLIC_KEY: ${VAPID_PUBLIC_KEY}
      VAPID_PRIVATE_KEY: ${VAPID_PRIVATE_KEY}
    volumes:
      # Secreto externo de GCP montado en runtime (solo lectura)
      - /data/coolify/secrets/gcs/service-account.json:/app/service-account.json:ro
    expose:
      - "5000"
    depends_on:
      postgres_db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    stop_grace_period: 30s
    mem_limit: 512m
    networks:
      alltura_network:
        aliases:
          - appandamios-backend
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsS http://127.0.0.1:${PORT:-5000}/health/ready >/dev/null || exit 1" ]
      interval: 30s
      timeout: 15s
      retries: 5
      start_period: 45s
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NODE_ENV: ${NODE_ENV:-production}
        VITE_IMAGE_MAX_MB: ${VITE_IMAGE_MAX_MB:-25}
    environment:
      BACKEND_URL: ${BACKEND_URL:-http://appandamios-backend:5000}
      SERVICE_FQDN_FRONTEND: ${SERVICE_FQDN_FRONTEND}
      SERVICE_URL_FRONTEND: ${SERVICE_URL_FRONTEND}
    expose:
      - "80"
    depends_on:
      backend:
        condition: service_started
    restart: unless-stopped
    stop_grace_period: 30s
    mem_limit: 128m
    networks:
      - alltura_network
      - coolify
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsS http://127.0.0.1:80/ >/dev/null || exit 1" ]
      interval: 30s
      timeout: 15s
      retries: 4
      start_period: 20s
networks:
  alltura_network:
    driver: bridge
  coolify:
    external: true
volumes:
  postgres_data:
  redis_data:
